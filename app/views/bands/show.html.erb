<section class="hero is-left">
  <div class="hero-content is-gradient">
    <div class="container is-fluid">
      <h1 class="title is-1 artist-page-title">
        <%=@band.name%>
      </h1>
    </div>
  </div>
</section>
<div class="container">
  <div class="columns is-wrap is-columns-margin">	
  	<div class="column is-4">
  		<%= render 'menu'%>
      <a class="button is-warning">Update</a>
      <a class="button is-danger" data-method="DELETE" data-confirm="Are you sure?" href=<%="#{user_band_path}"%>>Delete</a>
  	</div>
  	<div class="column is-8">
  		<div class="content">
        <h1 class="title is-3">Description</h1>
        <p><%= @band.description %></p>
      <hr>
        <h1 class="title is-3">Cache</h1>
        <p><%= @band.cache %> €</p>
        <p><strong>Management Commission: </strong> <%= @band.comission %>%</p>
      <hr>
      </div>
      <%= render 'members'%>
  	</div>
  </div>
</div>
